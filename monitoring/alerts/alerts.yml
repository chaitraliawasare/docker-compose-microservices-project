groups:
- name: app-alerts
  rules:
  - alert: AppDown
    expr: up{job="app"} == 0
    for: 1m
    labels:
      severity: critical
    annotations:
      summary: "App is down"
      description: "Prometheus has detected that the app is not responding for more than 1 minute. {{$labels.instance}} is down."